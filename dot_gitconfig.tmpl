[user]
	name = Nikhil Prabhu
	email = {{ .email | quote }}
	signingkey = {{ .signingkey | quote }}
[commit]
	gpgsign = true
{{- if eq .chezmoi.os "darwin" }}
[gpg]
	program = /opt/homebrew/bin/gpg
{{- end }}
